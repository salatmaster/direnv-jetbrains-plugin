<idea-plugin require-restart="false">
    <extensions defaultExtensionNs="com.intellij">
        <!-- Suggests, never applies silently: a Nix store path can vanish between reloads. -->
        <postStartupActivity implementation="io.github.salatmaster.direnv.java.DirenvJdkSuggester"/>
    </extensions>
</idea-plugin>
